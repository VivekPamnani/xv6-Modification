#include "types.h"
#include "x86.h"
#include "defs.h"
#include "date.h"
#include "param.h"
#include "memlayout.h"
#include "mmu.h"
#include "proc.h"

int
sys_fork(void)
{
  return fork();
}

int
sys_exit(void)
{
  exit();
  return 0;  // not reached
}

int
sys_waitx(void)
{
  int * wait_time;
  int * run_time;
  argptr(0,(void*)&wait_time,sizeof(wait_time));
  argptr(1,(void*)&run_time,sizeof(run_time));
  return waitx(wait_time,run_time);
}

int 
sys_getpinfo(void)
{
  struct proc_stat* stat_1;
  int pid;
  argptr(0,(void*)&stat_1,sizeof(stat_1));
  argptr(1,(void*)&pid,sizeof(pid));
  return getpinfo(stat_1,pid);
}

int 
sys_SetPriority(void)
{
int priority_value;
argptr(0,(void*)&priority_value,sizeof(priority_value));
return SetPriority(priority_value);
}

int
sys_wait(void)
{
  return wait();
}

int
sys_kill(void)
{
  int pid;

  if(argint(0, &pid) < 0)
    return -1;
  return kill(pid);
}

int
sys_getpid(void)
{
  return myproc()->pid;
}

int
sys_sbrk(void)
{
  int addr;
  int n;

  if(argint(0, &n) < 0)
    return -1;
  addr = myproc()->sz;
  if(growproc(n) < 0)
    return -1;
  return addr;
}

int
sys_sleep(void)
{
  int n;
  uint ticks0;

  if(argint(0, &n) < 0)
    return -1;
  acquire(&tickslock);
  ticks0 = ticks;
  while(ticks - ticks0 < n){
    if(myproc()->killed){
      release(&tickslock);
      return -1;
    }
    sleep(&ticks, &tickslock);
  }
  release(&tickslock);
  return 0;
}

// return how many clock tick interrupts have occurred
// since start.
int
sys_uptime(void)
{
  uint xticks;

  acquire(&tickslock);
  xticks = ticks;
  release(&tickslock);
  return xticks;
}
